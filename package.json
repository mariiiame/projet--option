{
  "name": "blog-projet-option",
  "private": true,
  "description": "INSA Blog - backend + frontend",
  "scripts": {
    "backend": "mvn -f backend/blog-api/pom.xml spring-boot:run",
    "frontend": "npm run dev --prefix frontend/blog-app",
    "dev": "concurrently --names \"api,web\" -c \"blue,green\" \"npm run backend\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
